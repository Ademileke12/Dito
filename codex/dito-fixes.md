# 🛠️ Dito Fix Prompts

Use these prompts with any AI (ChatGPT, DeepSeek, Claude) to fix the issues found in your code.

---

## Fix #1: Hardcoded JWT Secret

**Issue**: The `JWT_SECRET` is hardcoded in the `constants.ts` file.

### 📋 Copy & Paste Prompt:
```
Act as a Senior Security Engineer. I have a code issue in my project.

ISSUE: Hardcoded JWT Secret - The `JWT_SECRET` is hardcoded in the `constants.ts` file.

TASK: Analyze the relevant files in my project and rewrite the code to fix this issue according to industry best practices. Ensure the fix is secure and efficient.
```

---

## Fix #2: Hardcoded API URL

**Issue**: The `API_URL` is hardcoded in the `constants.ts` file.

### 📋 Copy & Paste Prompt:
```
Act as a Senior Security Engineer. I have a code issue in my project.

ISSUE: Hardcoded API URL - The `API_URL` is hardcoded in the `constants.ts` file.

TASK: Analyze the relevant files in my project and rewrite the code to fix this issue according to industry best practices. Ensure the fix is secure and efficient.
```

---

## Fix #3: Potential SQL Injection

**Issue**: The `axios.post` request in the `QuizModal.tsx` file is vulnerable to SQL injection attacks.

### 📋 Copy & Paste Prompt:
```
Act as a Senior Security Engineer. I have a code issue in my project.

ISSUE: Potential SQL Injection - The `axios.post` request in the `QuizModal.tsx` file is vulnerable to SQL injection attacks.

TASK: Analyze the relevant files in my project and rewrite the code to fix this issue according to industry best practices. Ensure the fix is secure and efficient.
```

---

## Fix #4: Potential XSS

**Issue**: The `axios.post` request in the `QuizModal.tsx` file is vulnerable to XSS attacks.

### 📋 Copy & Paste Prompt:
```
Act as a Senior Security Engineer. I have a code issue in my project.

ISSUE: Potential XSS - The `axios.post` request in the `QuizModal.tsx` file is vulnerable to XSS attacks.

TASK: Analyze the relevant files in my project and rewrite the code to fix this issue according to industry best practices. Ensure the fix is secure and efficient.
```

---

## Fix #5: Lack of Rate Limiting

**Issue**: There is no rate limiting implemented in the codebase.

### 📋 Copy & Paste Prompt:
```
Act as a Senior Security Engineer. I have a code issue in my project.

ISSUE: Lack of Rate Limiting - There is no rate limiting implemented in the codebase.

TASK: Analyze the relevant files in my project and rewrite the code to fix this issue according to industry best practices. Ensure the fix is secure and efficient.
```

---

## Fix #6: Lack of Error Handling

**Issue**: There is no proper error handling implemented in the codebase.

### 📋 Copy & Paste Prompt:
```
Act as a Senior Security Engineer. I have a code issue in my project.

ISSUE: Lack of Error Handling - There is no proper error handling implemented in the codebase.

TASK: Analyze the relevant files in my project and rewrite the code to fix this issue according to industry best practices. Ensure the fix is secure and efficient.
```

---

